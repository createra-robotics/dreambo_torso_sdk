# Template for the repo-local .env file. Copy to `.env` and fill in real
# values; the actual `.env` is gitignored and stays on your machine.

# ModelScope access token. Required by scripts/emotion_library/record.py
# Get one from https://www.modelscope.cn/my/myaccesstoken — needs write
# scope on the target dataset.
MODELSCOPE_API_TOKEN=
